version = "1.0.0.0";

source_tool_name = "NEPLAN";

/* File Path Configuration*/
files = {
    input_directory_path = "src/";

    //for cmake
    output_directory_path = "./"; 
    template_directory_path = "./";
    
      //for developing using makefile
    //output_directory_path = "./build/bin/"; 
    //template_directory_path = "./build/bin/";

};

/* System Configuration*/
system = {
    // Enable Setting
    enable = true;
    topology_trans_parameter = [1.0, 0.0, -1.0, 0.0];//parameters for convert the coordinate system , CIMObjectHandler::convert_coordinate(double x, double y)
    coordinate = [0.0, 0.0, 400.0, -400.0];//modelica coordinate
    label = {
        name = "System";
        simMode = "steady";
        init = "steady";
        annotation = {
            extent = [0.0, -30.0, 30.0, 0.0];//label size
            visible = true;
        };
    };
    annotation = {
        extent = [160.0, -160.0, 180.0, 260.0];
    };
};

single_phase = {

    /* Slack Configuration*/
    slack = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-5.0, -5.0, 5.0, 5.0];
            };
            visible = true;
        };
    };

    /* BusBar Configuration*/
    busbar = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-8.0, -8.0, 8.0, 8.0];
            };
            visible = true;
        };
    };

    /* PiLine Configuration*/
    piline = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-8.0, -8.0, 8.0, 8.0];
            };
            visible = true;
        };
    };

    /* RxLine Configuration*/
    rxline = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-8.0, -8.0, 8.0, 8.0];
            };
            visible = true;
        };
    };

    /* Transformer Configuration*/
    transformer = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-8.0, -8.0, 8.0, 8.0];
            };
            visible = true;
        };
    };

    /* PQLoad Configuration*/
    pqload = {
        // Enable Setting
        enable = true;
        use_profiles = false;
        profile_name = "NoName";//Defaul NoName
        profile_filename = "NoName";
        annotation = {
            transformation = {
                extent = [-8.0, -8.0, 8.0, 8.0];
            };
            visible = true;
        };
    };

    /* ZLoad Configuration*/
    zload = {
        // Enable Setting
        enable = true;
        use_profiles = true;
        profile_name = "NoName";
        profile_filename = "NoName";
        annotation = {
            transformation = {
                extent = [-8.0, -8.0, 8.0, 8.0];
            };
            visible = true;
        };
    };

    /* SolarGenerator Configuration*/
    solar_generator = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-5.0, -5.0, 5.0, 5.0];
            };
            visible = true;
        };
    };

    /* WindGenerator Configuration*/
    wind_generator = {
        // Enable Setting
        enable = true;
        annotation = {
            transformation = {
                extent = [-5.0, -5.0, 5.0, 5.0];
            };
            visible = true;
        };
    };
    
    /* Battery Configuration*/
    battery = {
        // Enable Setting
        enable = true;
        type = "type0";//type0, type1
        annotation = {
            transformation = {
                extent = [-5.0, -5.0, 5.0, 5.0];
            };
            visible = true;
        };
    };
    
    /* Household Configuration*/
    household = {
        enable = true;
        use_households = false;
        type = "type1";//type0, type1, type2
        annotation = {
            transformation = {
                extent = [-5.0, -5.0, 5.0, 5.0];
            };
            visible = true;
        };
    };
};
